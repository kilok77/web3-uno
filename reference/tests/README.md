# Supplied Tests

This directory contains the original `oo model.zip` and `functional-test.zip` archives. Keep both unchanged.

The OO archive is extracted to `oo-model/` for inspection. Keep that extraction pristine; future implementation should use an assignment-local harness/copy when adapter code must be completed. Do not edit supplied assertions merely to make an implementation pass.
